// import { Ndb2Client } from "..";

// export const generateGetUser = (client: Ndb2Client) => {
//   const getUser = async (discordId: string) => {
//     try {
//       const { id } = await client.fetchUserId(discordId);
//       return await client.fetchEnhancedUser(2);
//     } catch (err) {
//       throw err;
//     }
//   };

//   return getUser;
// };
